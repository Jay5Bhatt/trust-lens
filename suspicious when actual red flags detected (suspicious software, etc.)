[1mdiff --git a/src/lib/combined-analysis.ts b/src/lib/combined-analysis.ts[m
[1mindex 0605ef9..ddcb88c 100644[m
[1m--- a/src/lib/combined-analysis.ts[m
[1m+++ b/src/lib/combined-analysis.ts[m
[36m@@ -50,7 +50,8 @@[m [mfunction convertToAnalysisResult([m
     const lowerAnomaly = anomaly.toLowerCase();[m
     [m
     if (lowerAnomaly.includes("no exif") || lowerAnomaly.includes("missing")) {[m
[31m-      severity = exifResult.suspicious ? "High" : "Medium";[m
[32m+[m[32m      // Missing EXIF is common and not necessarily suspicious - mark as Low severity[m
[32m+[m[32m      severity = "Low";[m
     } else if (lowerAnomaly.includes("suspicious")) {[m
       severity = "High";[m
     }[m
[36m@@ -122,9 +123,9 @@[m [mfunction convertToAnalysisResult([m
       color = "red";[m
     } else if (geminiResult.overall_verdict === "likely_real") {[m
       score = Math.floor(geminiResult.confidence * 20) + 80; // 80-100 range[m
[31m-      // Lower score if EXIF is suspicious[m
[32m+[m[32m      // Only lower score if EXIF has actual suspicious indicators (not just missing data)[m
       if (exifResult.suspicious) {[m
[31m-        score = Math.max(60, score - 15);[m
[32m+[m[32m        score = Math.max(70, score - 10); // Less penalty, trust Gemini more[m
       }[m
       status = "LIKELY AUTHENTIC";[m
       color = "green";[m
[36m@@ -140,12 +141,14 @@[m [mfunction convertToAnalysisResult([m
   } else {[m
     // Gemini failed, use EXIF only[m
     if (exifResult.suspicious) {[m
[32m+[m[32m      // Only show suspicious if there are actual red flags, not just missing EXIF[m
       score = Math.floor((1 - exifResult.confidence) * 40) + 20; // 20-60 range[m
       status = "SUSPICIOUS METADATA";[m
       color = "yellow";[m
     } else {[m
[32m+[m[32m      // Missing EXIF alone is not suspicious - show as informational[m
       score = Math.floor(exifResult.confidence * 30) + 60; // 60-90 range[m
[31m-      status = "METADATA CHECK PASSED";[m
[32m+[m[32m      status = exifResult.hasExif ? "METADATA CHECK PASSED" : "METADATA ANALYSIS COMPLETE";[m
       color = "green";[m
     }[m
   }[m
[1mdiff --git a/src/lib/exif-analysis.ts b/src/lib/exif-analysis.ts[m
[1mindex 94ffb8c..a077f22 100644[m
[1m--- a/src/lib/exif-analysis.ts[m
[1m+++ b/src/lib/exif-analysis.ts[m
[36m@@ -42,13 +42,13 @@[m [mexport async function analyzeExifMetadata(file: File): Promise<ExifAnalysisResul[m
 [m
             if (!hasExif) {[m
               URL.revokeObjectURL(url);[m
[31m-              anomalies.push("No EXIF metadata found - image may have been stripped or generated");[m
[32m+[m[32m              anomalies.push("No EXIF metadata found - common for screenshots and processed images");[m
               resolve({[m
                 hasExif: false,[m
                 anomalies,[m
                 metadata: {},[m
[31m-                suspicious: true,[m
[31m-                confidence: 0.6,[m
[32m+[m[32m                suspicious: false, // Missing EXIF alone is not suspicious - it's very common[m
[32m+[m[32m                confidence: 0.7, // Slightly lower confidence but not suspicious[m
               });[m
               return;[m
             }[m
[36m@@ -95,13 +95,9 @@[m [mexport async function analyzeExifMetadata(file: File): Promise<ExifAnalysisResul[m
               metadata.compression = compression.toString();[m
             }[m
 [m
[31m-            // Check for missing critical metadata[m
[31m-            if (!make && !model) {[m
[31m-              anomalies.push("Missing camera make/model information");[m
[31m-            }[m
[31m-            if (!dateTimeOriginal && !dateTime) {[m
[31m-              anomalies.push("Missing date/time information");[m
[31m-            }[m
[32m+[m[32m            // Note missing metadata but don't treat as anomalies (common for processed images)[m
[32m+[m[32m            // Only add as informational if we have some EXIF but missing these specific fields[m
[32m+[m[32m            // (If we have EXIF data, we can note missing fields, but it's not necessarily suspicious)[m
 [m
             // Check for location data[m
             const latitude = EXIF.getTag(this, "GPSLatitude");[m
[36m@@ -110,8 +106,13 @@[m [mexport async function analyzeExifMetadata(file: File): Promise<ExifAnalysisResul[m
               metadata.location = `GPS: ${latitude}, ${longitude}`;[m
             }[m
 [m
[31m-            // Determine if suspicious[m
[31m-            const suspicious = anomalies.length > 0 || !metadata.camera || !metadata.date;[m
[32m+[m[32m            // Determine if suspicious - only mark as suspicious if there are actual red flags[m
[32m+[m[32m            // Missing camera/date alone is not suspicious (common for processed images)[m
[32m+[m[32m            // Only suspicious if there's suspicious software or other actual indicators[m
[32m+[m[32m            const hasSuspiciousSoftware = anomalies.some(a => a.toLowerCase().includes("suspicious software"));[m
[32m+[m[32m            const suspicious = hasSuspiciousSoftware || anomalies.some(a =>[m[41m [m
[32m+[m[32m              a.toLowerCase().includes("suspicious") && !a.toLowerCase().includes("no exif") && !a.toLowerCase().includes("missing")[m
[32m+[m[32m            );[m
 [m
             // Calculate confidence (higher if has complete metadata, lower if anomalies)[m
             let confidence = 0.8;[m
