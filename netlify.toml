[build]
  functions = "netlify/functions"
  publish = "dist"
  command = "npm run build"

# Externalize heavy dependencies from function bundles to reduce size
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pdf-parse", "mammoth", "@google/genai"]

